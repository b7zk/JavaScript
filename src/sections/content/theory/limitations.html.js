export default `
<h1> JS Limitations</h1>
<ul>
<li>JavaScript is single-threaded, meaning it can only execute one task at a time.</li>
<li> Doesn't allow reading or writing files directly on the user's system for security reasons.</li>
<li>Limited access to system resources, such as hardware or network interfaces.</li>
<ul>
`;
